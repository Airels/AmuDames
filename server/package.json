{
  "name": "amudames",
  "version": "1.0.0",
  "description": "Un simple jeu de dames avec Angular et NodeJS",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel server/ -d dist && rm -rf dist/app && cp -r app/ dist/",
    "start": "./load_front.sh && npm run build && node dist/server.js",
    "inspect": "node --inspect dist/server.js"
  },
  "keywords": [
    "dames",
    "amu",
    "amudames"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@elastic/elasticsearch": "^7.11.0",
    "express": "^4.17.1",
    "express-async-handler": "^1.1.4",
    "express-session": "^1.17.1",
    "rxjs-compat": "^6.6.3",
    "ws": "^7.4.3"
  },
  "devDependencies": {
    "@babel/cli": "^7.12.16",
    "@babel/core": "^7.12.16",
    "@babel/preset-env": "^7.12.16"
  }
}
